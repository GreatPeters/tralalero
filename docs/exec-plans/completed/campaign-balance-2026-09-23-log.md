# Campaign balance execution log

- Official Pipeline and narrow scene query succeeded. Took a new 66-key preference snapshot before testing; wallet 1781 coins, 0 jewels.
- Existing feedback depends on HUD value comparison and emits a world-space number for 0.75 game seconds plus a short edge flash. A controlled HighWay prop-type hit does display it in the first frame. No screen-space amount/cause exists; continue investigating missed/delayed paths.
- Earlier 20-run review used stat fixtures and did not clear RestStop. September 12 cohorts used obsolete lane-dodging behavior. Neither establishes current campaign balance.

## Damage feedback implementation

- `ApplyDamage` now reports the exact committed loss before HUD refresh. `DamageTaken` permits event-level auditing. Repeated UI refreshes do not emit duplicate notices; legacy direct loss retains a fallback. The missile-count tradeoff now uses typed damage and cannot resurrect a dead player to 1 HP.
- A reused screen-space notice shows amount and cause for 1.55 real seconds, alongside the original red edges and shark-local number. Native SR18 frames show contact and 100-HP crate notices. Layout refinement and physical highway-prop capture remain pending.
- Two new focused tests pass, covering every damage enum, overkill, dead callbacks, healing, invalid values and legacy writes. Runtime C# build passes with one existing third-party warning.

## Cohort instrumentation

- `CampaignSave` resets only test progression, uses real `UpgradeUI.TryBuy`, and restores the original wallet/levels/cosmetics/tutorial. Ten extra chapter-workshop keys are separately backed up; the old 66-key helper did not include them.
- Baseline-01 stopped before mutation because native Roslyn sees two JsonConvert types. Replaced the direct type reference with the existing assembly-qualified reflection pattern. Baseline-02 contains five diagnostic runs (34.6–49.4 seconds), then restoration.
- Found a steering defect: after defeating one member of a pair, the old policy aimed at the other and could ram both. The campaign policy commits to a chosen opening. It still uses normal `PlayerMove` once per FixedUpdate, matching the real input path, with no health override or disabled collisions. Baseline-03 uses this corrected policy, independent run seeds and 3x simulation.
- Baseline-03 first 13 attempts: 34.8, 49.4, 59.3, 59.4, 71.3, 59.3, 71.2, 91.9, 92.0, 94.9, 106.2, 118.9, 139.9 seconds. Attempt 14 reaches 298.9 seconds with several bomber bonuses but dies to the final boss. Attempts 15/16 die at 288.6 seconds. These observations expose bonus variance and a possible late plateau; balance changes are not yet certified or installed.
- Existing user requirements explicitly make upright poles/holes/other major hazards lethal. Preserve this behavior while fixing damage presentation and telegraph fairness; do not silently reinterpret stale workbook damage descriptions as authority to remove instant death.

## Baseline result and first revision

- Baseline-03 completed 30 real attempts. Chapter 1 first clear: attempt 23, 305.5 seconds, entry HP1250 / ATT52 / levels11,14,6. Attempts14–22 mostly reach289–299 seconds without clearing. Carried progression into Highway: first attempt31.4seconds, then57.3,66.2,42.8,90.6,90.6,100.5seconds. No free currency, ads or stat overrides. Original preferences restored afterward.
- Found a dead workbook control: C18 was disconnected from all21left normal-enemy growth inputs, which were fixed10%. R1 connects those P cells, raises normal growth to16%, lowers the separate boss multiplier to1.31 while preserving its1.5buff, and reduces late price coefficient3.1→2. Result: final ordinary HP723→1885, elite1301→3393, final Woman3687→3704. Goal cells now say12seconds /25attempts with10–15seconds and20–30attempt ranges. This candidate still needs earned-progression acceptance.
- R1 installed natively and Data.bytes validated. SHA256 `e7a9f4acc76589ab6faaf0d3cb50d73b9988366a667fe315887b9b93652ec456`. Graft preserves unrelated cells/styles/parts and exact formula text; only authored cells and transitive formula caches change. Artifact reports pre-existing unquoted-Korean MAX errors at C92/C93, so their original60caches remain untouched. Shared-formula followers require relative-reference resolution for dependency analysis; the original shared XML is preserved.
- First oncoming traffic collided with the first compulsory enemy funnel: dodging the center car forced aim outside the enemy capsule. Move its trigger28→84m, placing the meeting around118m between rows64m/155m. Authoring builder and saved scene agree.
- Toll arms now animate, amber-warn0.9seconds before closure, and open the next lane in advance. Contact follows physical arm clearance; four tests verify a passable lane continuously at1/6/7-second cycles and early-opening behavior.
- Smaller enemy-contact labels and a damage notice below the HUD keep the approach road visible. A real Roadblock collider relocated to the stationary ordinary-health player produced500→0, causeRoadblock, exactly one notification and visible `-500 HP`. This is a physical-contact fixture, not a traversal/balance run.
- Read-only three-scene content audit:50/50/66actors, one Animator each. Three RestStop-specific roles have2.26m bodies and no recoil marker, noticeably smaller than the3.1m reconstructed shared roles. Native polish/capture remains pending.
- R1 variable-frame calibration is archived separately. Fixed30game-fps cohorts will remove Editor frame-cadence variation from subsequent comparisons; they remain simulation evidence, not device FPS measurements.

## R1 fixed-cadence cohort and RestStop preparation

- `r1-fixed` is running, stopping after chapter1clear for staged review; CLI session48355. `Time.captureDeltaTime=1/(30*3)` was verified to yield game delta0.0333333 with ordinary physics delta0.02. Restore resets capture timing to0. R1 variable-frame five-run calibration is retained in `r1-campaign`.
- R1 attempts19/20reach264seconds;21reaches281seconds;22reaches288.6seconds. No final R1clear certification yet.
- Changed only the temporary GameView display to540×1170 during run22, retaining the same portrait aspect, fixed game cadence and ordinary input/physics. This reduces capture/render load; it does not change shipped quality. Original selected GameView index23 /1080×2340 is saved in `tmp/campaign-balance-2026-09-23/gameview-before.txt`. Restore it at task completion. Added custom QA resolution is index26, label `Campaign QA 540x1170`.
- RestStop3motion revisionv2 is built outside Assets, still UNINSTALLED. Source bodies/textures and18bones preserved; seven actions include forward tool gesture, recoil and complete death. Coffee right hand now reaches its forward peak at0.6s, Y−0.479 instead of the old backwardY+0.57. Native cup/stop-paddle/spatula installer is prepared and dry-compiled only.
- Nine Blender inspections (authored/FBX/freshGLB for3roles) pass with no issues. Body-onlyGLB excludes hidden legacy procedural equipment; FBX retains hidden node IDs for stable Unity imports. Actual multiview/critical-frame images were opened. The initial fresh-GLB render included invisible importer bone-shape bounds (`glTF_not_exported`) and misleadingly floated the floor; local `render-campaign-evidence.py` excludes those gizmos and uses evaluated geometry. The correct images are `fresh-glb-clean`, not the earlier diagnostic folders.

### Pending focused review corrections

- Aggregate different-source damage arriving within one short burst, so a tiny second hit cannot replace a large first screen notice immediately.
- Inspect/fix equal-health player/enemy contact: current else branch can leave enemy health0without calling EnemyDeath.
- Exclude explicitly disabled placements and holdout-controlled police from campaign steering targets, while retaining legitimately cleared passage choices. Do not treat phantom targets or a weak aiming policy as balance evidence.
- Native-install the3RestStopmotion assets, fit and visually verify tools, set Coffee workbook release rows to0.6s, and verify physical release position/damage. The current installer has not run.
- Confirmed additional healing/death hazards in source: `ApplyRunHealthBonus`, direct bypass/holdout healing and regeneration can raise a zero-health player before Canvas observes death. Add an alive-only Heal transaction, use it for positive recovery, preserve explicit run reset, and make fatal damage win over simultaneous finish-line completion. Test dead bonus/heal/recovery and zero-health victory rejection.

## Chapter 1 accepted cohort and subsequent fixes

- R1 fixed-cadence chapter1first clear: run30 at305.5seconds. Starting HP1760, ATT60, levels13/17/7. Bank245after the actual purchase,380earned,625carried out. There were27purchase visits; endpoint survival growth divided by purchase visits is10.026seconds. This is a cohort-level measure, not a claim that every purchase or random run adds exactly10seconds. Full per-run curves and plateaus remain in the evidence.
- Native-installed three RestStop roles into their source prefabs and30placements. Seven clips, ~3.05m rendered height, recoil and1.1sdeath lifetime. Coffee uses a visible project cup at the real palm; chef spatula leans forward so the arm cannot hide its blade; parking stop paddle remains readable. Final native previews: CoffeeVendor/SnackChef `showcase-native-v2`, ParkingMarshal `showcase-native-v1`.
- Coffee ten workbook release rows now0.6s. Current canonical workbook `r1-motion` SHA256 `6e0ac12fee0abbe6d198d8b0b962505142107bf86bc6032740f9ee6320e3c16b`; protected archive verified. Chapter1balance unchanged from R1.
- Controlled native coffee probe: release0.6s, first shot0.6s, positional/rotational discontinuity0, ordinary player500→400, causeEnemyProjectile. Actor HP10000/attack100 and movement held only for this diagnostic; not a balance run.
- Added alive-only Heal transaction for regeneration, bypass/holdout recovery and health bonuses; fatal damage now outranks finish-line victory. Equal-health physical player/enemy contact500/500 now leaves both0and enemyStateDead. Three lifecycle tests and native fatal-heal/win probes pass.
- Combined player damage uses one reserved slot within the original64-popup pool, so200enemy popups cannot overwrite it. Mixed-source burst100+25 shows one−125above the shark plus a footer notice, preserving the tutorial/route view. Popup-pool5, run-balance6 and road-pattern20tests pass.
- Crucial reproduction: after script/scene startup hitch, the first footer probe returned an empty notice even though health fell. Its first LateUpdate charged pre-hit unscaled frame time and expired the new notice before rendering. Both player indicators now skip their receipt frame and cap only presentation decay at0.1s/frame. Six damage-notification tests pass; repeated native hitch probe shows375/500,−125world text and footer, while dead healing/victory remain rejected. Use `lifecycle-burst-heal-hitch-safe` as final evidence, not `lifecycle-burst-heal-final` (retained failed attempt).
- Resumed chapter2with earned carryover. Runs31/32/33die at57.3/57.3/66.5seconds, earning1350/1350/1650coins. Paused after33to finish the hitch fix, restored user state, then resumed from state33. Active CLI session is96122's successor (see current tool output), stopping after chapter2clear. Disabled authoring placements and holdout-controlled police are excluded from steering targets.

## Curved-road projectile correction and current campaign

- R1 chapter 2 eventually cleared on global run 46 (16 chapter attempts), 298.6 seconds, before the projectile-path correction. This remains a diagnostic cohort, not the final chapter 2 acceptance.
- Found that friendly bullets moved in a straight line at their initial height and received only the owner's rotation delta. They therefore did not follow the road at their own position, especially on curves and slopes. `HighwayProjectilePath` is now a per-projectile value type preserving launch position, lane, road clearance, spread and launch branch while advancing independently of the player. Stationary defense retains free aim; Noryangjin's existing discrete turn behavior remains. Five trajectory/integration tests and 13 combat-route tests pass.
- Found source-prefab throw timing differed from workbook timing. `HighwayEnemyAnimation.PrepareThrow` retimes the attack to reach its authored strike phase at the configured delay, then restores normal speed. Coffee uses phase0.5; existing highway roles use0.46. Native Tire probes with workbook-like0.55/0.8-second delays released at0.5667/0.8seconds, phases0.474/0.46, zero pose discontinuity, and both hit for100HP. Legacy pooled enemy death is now idempotent until re-enable.
- Chapter2 enemy and progress checkpoint rewards are100 (previous150), reflecting the revised upgrade prices and desired growth pace. Current workbook revision `r2-road`, SHA256 `6a848d37ba9e77847714155094fb7a9931f23420fd1a7edf9e0df148e7738171`; runtime archive verified. Other stage rewards unchanged at this point.
- `r2-road` contains seven diagnostic chapter2attempts with the old tangent-based aiming policy. The final candidate `r3-route-aware` reuses only the unchanged, verified first30chapter1records and state30 via hard links; those are not new attempts. Its aiming uses target road lanes on curved maps. Wallet reconciliation passes for the carried state and subsequent real purchases. Active CLI session39495 runs through chapter3or global attempt100.
- A 12-game-second native segment was recorded from run44 without camera/stat/gameplay changes. Its timeline confirms E13 ConeMechanic1286→0 from shooting before contact while player HP stays388; this is a legitimate cleared passage, not an escape through live enemies. Segment: `segments/고속도로-커브사격/`.
- The current cohort is still in chapter2. Update final counts and acceptance only after actual clears. Continue with `python tools/analyze-campaign-cohort.py <cohort>` and retain failed/calibration folders separately.

## Chapter 2 accepted; chapter 3 contact and entry calibration

- `r3-route-aware` cleared chapter2on global run49:19chapter attempts,298.57seconds, first attempt42.8seconds. There were18purchase visits after that first attempt, giving14.209endpoint survival seconds per subsequent purchase visit. Actual chapter-completion flag and unlock3were recorded. Final wallet5537 was carried forward.
- Early chapter3diagnostics exposed real double contact: run50took2915HP from one chef and1729HP from its partner within the same19.1-second crossing. Sideways patrols were converging. Twenty patrol placements now move forward/back in their own lanes;25pairs share the existing one-contact claim. No highway funnel geometry was added to RestStop. ChapterProgression resets scene contact claims each run. Saved-scene integration1and existing contact-claim8tests pass.
- RestStop does not contain a HighwayRoute component; its authored road uses the older movement path. The first contact installer correctly failed before mutation when it assumed that component existed. It now uses the Noryangjin_MapTool root. Keep this distinction in future automation.
- `r4-contact-pairs` reuses the accepted first49records and contains16chapter3calibration attempts. Correct contact behavior improved19.1→27.6seconds, but the entry economy still stalled at27–45seconds despite actual purchases. Retained as diagnostics.
- Chapter3entry is now calibrated against real carried HP6350/ATT116 rather than the older11330HP fixture. Revision `r3-reststop`: first normal health2750→2000, per-station health growth6%→7.5% (retains a strong late stage), enemy/progress reward300→450. Existing elite/boss multipliers remain. Source/price settings for chapters1/2 are unchanged. Current workbook SHA256 `abdfad47d340f2deb19cec7441b8e842369046a1c2bbb8ffa82161dad6abb167`; native runtime archive verified.
- Active cohort: `r5-reststop`, copied only the unchanged accepted first49records/state49, then actual new chapter3attempts. Do not count copied records as additional tests. Active CLI session is the latest run-campaign tool result. Final chapter3clear remains pending.

## Additional chapter 3 blockers and current state

- Recorded a traffic steering defect at189.7seconds: the target lane was336.4m because a toll on a different parallel road passed the ahead-only filter. The bot then tried to cross the full road too late. Test steering now excludes laterally remote hazards, clamps its intended lane before collision prediction and gives crossing traffic32m of approach. In the game, the warning persists while the crossing vehicle moves. The vehicle remains lethal as requested.
- The initial chapter3growth7.5% still exceeded the legal core-upgrade response. Current revision `r4-reststop-growth` uses5% growth with first normal HP2000, enemy/progress reward450 and the existing elite/boss multipliers. Current workbook SHA256 `d8c6bd98aa6e4eec3b2440797fa70126d2a38f5b310039717e14bc4cf73af193`; archive verified. Earlier chapter1/2 data unchanged.
- Runtime inspection found E10 TireBruiser at lane−5.3m, outside the player's±4.4m movement range. The older generic alignment tool used the left actor as its center when RestStop had neither HighwayRoute nor EnemyCornerClearance; repeating it shifted the group by another1.1m. This was a real placement defect, not insufficient stats.
- `RestStopChapterBuilder.ProjectRoadCenter` now projects onto the canonical road polyline. All25pairs are placed at±1.1m from that center, preserving longitudinal stations and height. The old combat-alignment tool uses the same projection. Applying the repair twice and checking the saved scene passes two integration/idempotence tests. Patrol lanes and one-contact claims remain intact.
- `r6-reststop-final` retained19chapter3diagnostic attempts before this geometry correction. The current candidate is `r7-centered-reststop`, reusing only the accepted first49records and rerunning chapter3from the real earned checkpoint. Active process ID is in the latest tool output. Still require a real chapter3clear before completion.
- Browser preview is open in Chrome tab1194368272 (`campaignGallery`) at `http://127.0.0.1:6753/campaign-balance/`. It is explicitly labeled chapter3verification in progress. The page and a native character video were visually checked; update it with final results and reverify before delivery.

## Final earned campaign and comparison results

- R7 centered RestStop cleared on global56 (chapter attempt7) at299.4s. Since endpoint progression was faster than the10–15s target, the final revision reduces RestStop enemy/progress rewards450→350 without further changing enemies or purchases.
- Final workbook `r5-reststop-rewards`, SHA256 `fcdd4fe5c8dfe4947f7adafdf9c0d4a751a6af464a71bcccaf05c70f551558ff`. Source schema and protected archive verified; unrelated workbook cells/parts preserved.
- R8 final rewards again clears on global56: chapter counts30/19/7, durations305.5/298.57/299.43s. Chapter3first attempt187.13s, final bank13137. All wallets reconcile, completion flags are true, and original preferences were restored by the campaign driver. R8 reuses only the accepted first49records, not additional new attempts.
- Endpoint seconds gained per post-first purchase visit are10.026/14.209/18.717. Combined12.525across51visits. Chapter3remains faster; do not assert all three independently meet10–15s or call a multi-upgrade visit a single upgrade.
- Six additional same-seed runs compare actual purchases at chapter checkpoints12/38/53. Results106.23→106.23s,100.5→100.5s and248.53→274.83s. This shows two same-wave plateaus and a26.3sjump. It is evidence against a guaranteed fixed per-purchase gain, not evidence to hide or reinterpret as10–15s.
- Final review caught the legacy HealthBoost trigger still able to consume a wall after fatal damage. The trigger now rejects zero health before any choice/touch mutation. Three added cases cover legacy/normal/unique health pickups. Final22-suite regression matrix194/194; runtime/editor builds and harness pass. The editor build retains a third-party SplineSpeed unassigned-field warning.
- Native footage exposed an unreadable bare RestStop bend. Added five direction boards and ten outside trees through `tools/install-reststop-corner-guides.cs`, with zero active colliders. An actual traversal clip at157–171s confirms left-pointing chevrons before the left turn. Existing repeated scenery and helper formation are retained.
- `iteration_review.json` retains the repaired RestStop motions after nine source/FBX/GLB hard-gate passes and native visual/throw checks. Incorrect importer-gizmo render bounds remain labeled failed diagnostics.
- The initial result capture waited1.3s, before the real victory/transition UI was ready. Updated the harness to2.6s and the campaign driver to3.5s before cleanup. Measured clear-state/seed replays now verify the actual delayed result panels; these are separate replays, not extra attempts in the30/19/7cohort. Chapter1repeated305.5s and its actual next-chapter/+20jewel panel is visibly confirmed. Chapters2/3replay and final view restoration are pending at this log entry.

## Completion

- All three clear replays completed at305.47/298.57/299.43s. Native PNG inspection confirms next-chapter/+20, next-chapter/+25 and chapter-complete/+30jewel panels. The final RestStop battle has584native frames from the actual clear replay, with no stat/camera override.
- Restored and verified all77original preference records, coin1781/jewel0, clean HighWay Edit Mode, unpaused state, normal speed and captureDeltaTime0. GameView reflection separately confirms index23 and1080×2340. Runtime archive isCurrent; final workbook hashmatchesr5.
- Final records, CSV, comparisons,194-test summary, workbook edits, clear replays and restoration proof are retained under `map-concepts/campaign-balance-2026-09-23/`. Large PNG/video/checkpoint evidence remains in the preview folder. The gallery presents actual results and pacing limitations, with local PNGs available as secondary links.
- ce-compound headless documented the damage lifecycle and the later road-placement/driver/capture lessons. No subagents, merge, push, phone installation or claimed device FPS acceptance.
