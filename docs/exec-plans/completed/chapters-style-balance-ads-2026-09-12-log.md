# Execution log

## Initial inventory

- Source project Unity6000.2.6f1, URP17.2, Pipeline0.6.0-exp.1. Current permission profile is workspace-write; ordinary sandbox could not execute Unity CLI. Narrow read-only escalation was approved and confirmed process10140/Pipeline reachable, but `list_open_scenes` still times out. Previous unsaved-scene/test wait remains unresolved; user asked to inspect the Editor confirmation window.
- Existing chapter progression has chapter and nextScene fields and manual `LoadNextChapter`, with only route completion granting unlock. No rewarded-ad SDK found in manifest or gameplay scripts.
- Six current Highway enemy prefabs already have18-bone rigs and six actions, but their realistic silhouettes are precisely what the user wants replaced. Existing rest-stop is a Highway decorative plaza, built with `RestStopSceneBuilder`; do not mistake that for a separately playable chapter3.
- Existing fonts include KERISKEDU_B, NotoSansKR variable, Gmarket Sans and LiberationSans. None yet verified as CC0. User asked whether OFL is acceptable.
- TRELLIS source folder remains `C:/Users/ljh/Desktop/AI 프로그램/Trellis 자동화/결과물`; existing automation wrappers and rigging/validation scripts are reusable. Local Wan workflow exists under the Creative-AMD ComfyUI environment.
- Plan alternatives selected: toon3D rigged mascots; distinct themed route sequences; optional rewarded coin video after a round; bounded task-owned background generation. Awaiting user clarification on20attempt semantics, CPU environment and production AdMob IDs.

## Character reference and production start

- Generated and visually inspected nine isolated chibi reference images with built-in imagegen. Highway: ConeMechanic, TrafficPatrol, TollgateChief, TireBruiser, AsphaltWorker, DeliveryRider. Rest stop: SnackChef, CoffeeVendor, ParkingMarshal. Inputs live at `map-concepts/chapters-polish-2026-09-12/references/enemies/`; source paths for02–09 are in `references/manifest.json`, and01 came from generated image `exec-df178859-6b46-45c7-8c22-34dd6e314d1a.png`. Full-resolution copies are in `tmp/image-previews/chapters-polish-2026-09-12/`.
- Modeling contract written at `map-concepts/chapters-polish-2026-09-12/modeling-contract.md`. This is reference stage, not finished3D/rig acceptance.
- Added `tools/run-limited-generation.py`: applies only to its own process/children,4thread environment limits, maximum8logical CPUs, below-normal priority and8GiB free-RAM startup gate. Existing user-wide settings/processes are not changed. Backend remains stdlib-first so ComfyUI initializes torch/allocator correctly.
- Task-owned TRELLIS backend started successfully with existing independent UV math on8189, PID35480, exec session88851. Owner record `map-concepts/chapters-polish-2026-09-12/trellis-server-owner.json`. Detected R9700 with32624MiB VRAM,48517MiB RAM; about27GiB free before startup.
- Sequential nine-character batch started using `tools/generate-chapter-mascots.py`, PID26436, exec session5778. Owner record `trellis-batch-owner.json`. Production output `outputs/chapters-polish-2026-09-12/enemies/`, status in `task-status.json`. Settings1536resolution,2048textures,20ktriangle target,1.6m longest side, background removal enabled, tiled decoder, model unloading, no automatic quality retries. First job ConeMechanic was generating at last observation. Do not launch Wan concurrently with this GPU queue.
- Official escalated Unity scene query was retried after production start and still timed out. No scene mutation has been made in this revision. Pending user UI/font/balance/ads questions remain unanswered.

## Latest steering — rest-stop arrival scale and fear

- User corrected the rest-stop movie references: the shark must be larger than people, and people must be afraid of it. This steers U4 and does not cancel the broader14-request revision.
- Generated and visually inspected replacements `reststop-A-v2.png` and `reststop-B-v2.png` under the transitions reference folder. Both show a giant three-foot shark at roughly1.7–2times adult height and nearby people staring upward, recoiling and raising hands/trays. The shark is calm/curious. These are the only rest-stop images to use for upcoming Wan candidates; prior A/B are retained as rejected scale references. `references/reststop-correction.json` records provenance and motion direction.
- HighwayA/B first-frame references were also generated and reviewed, using the actual moved concept path `output/meshy_images/stage_02_1_highway_concept_batch_v1.png`. All four original candidates and the two rest-stop corrections are copied into full-resolution preview paths.
- TRELLIS had completed the first3mascots and was generating TireBruiser at last inspection. ConeMechanic source preview is cute and upright; fresh automatic mesh inspection reported18,449triangles, UVs and finite coordinates, but rig/action visual validation remains.
- Ads SDK11.5.0 and required EDM1.2.187 downloaded through Google's documented OpenUPM path and SHA512-verified; manifest now references local pinned tarballs. Runtime reward offer/service/settings and DefeatPresentation integration are drafted, not yet compiled/imported/tested in Unity because of the existing main-thread block. Test IDs come from the pinned SDK's RewardedAdController sample (Android5224354917), not the inconsistent web page's interstitial ID.
- `tools/rig-chapter-mascots.py` is an in-progress derivative with new sources, short-limb joint positions and ground compensation. It currently calls `mascot_equipment`, which still needs to be added before execution. Do not run this unfinished rig script yet.

## Production and offline implementation — September13 continuation

- Latest user correction supersedes the cinematic anatomy assumption: TWO legs plus one shoe-wearing tail that acts as the third support, not three separate legs plus a tail. The current four transition candidates must not be installed. Regenerate corrected references and then rerun Wan. The original user-provided shark image exists at `C:/Users/ljh/.codex/generated_images/019f22f4-e2cc-73b1-8fdf-68dd8b36147a/ig_012060245ab0a594016a4d081cf2a48191b9f821a03274172d.png` and is the identity reference.

- All nine TRELLIS bodies completed and passed the automation's mesh validation. The task-owned TRELLIS server was stopped after verifying its queue was empty (`trellis-shutdown.json`). New `mascot_equipment.py` supplies rigid, bone-weighted tools. Rigging is now executable; v2 has18bones and six actions, with a per-frame world-space ground check. The first version confused Root local axes with world Z and was rejected. First six v2 rigs are complete and the last three are being baked; visual/fresh-import acceptance remains pending.
- Audited29 Highway prop meshes. Seven explicitly reviewed semantic candidates49/54/57/67/68/69/80 have replacement exports in `outputs/chapters-polish-2026-09-12/props-v3`. Horizontal-face removal alone left a floor rim on car67, so the revision removes the slab band including its walls. Car67 v2 was visually accepted; all final v3 files still need their own visual review. Normal booth/camera/delineator physical bases were kept. No scene/prefab prop replacement has been applied yet.
- Added optional rewarded-ad settings/service/privacy action and a pure `RewardedCoinOffer` state machine. `DefeatPresentation` connects a per-round offer to the SDK earned callback; `CanvasScript` issues fresh round tokens and invalidates prior offers. Five actual NUnit assertions/cases were executed successfully in the isolated offline runner. SDK/native integration, serialized buttons and production IDs remain pending. Consent is requested before SDK initialization; failures have retry cooldowns and stale load callbacks are discarded.
- Added `ChapterTransitionUI` with bounded prepare/play waits and RenderTexture cleanup; `ChapterProgression` can automatically present a bound movie and load a valid next scene. Added `OpeningWorkshopPresentation` editor authoring using the existing upgrade workshop artwork and shorter story captions. These changes compile against current Unity references via `tmp/chapters-compile/AdditionalRuntime.targets`: runtime and Editor builds both have zero errors. They have not been imported or visually applied in Unity. A verified CC0 Korean font is still unresolved; the draft builder accepts a font override and must not be represented as satisfying that requirement yet.
- Wan server PID34576/session36867 and sequential batch PID19036/session59823 are task-owned and CPU-limited. Four5.04-second121-frame candidates use the installed Wan2.2 TI2V5B FP16 model,576x1024,24steps. Latents are saved before unloading diffusion and decoding. HighwayA finished sampling270.1s/decoding60.0s with full VAE decode; HighwayB is decoding. Only corrected giant-shark rest-stop v2 references are queued. No candidate has been visually accepted or installed yet.
- Official Unity MCP became callable in this continuation, but `list_open_scenes` still timed out after60000ms, consistent with the prior CLI main-thread block. Do not label offline builds as Unity Play Mode tests or claim the three requested gameplay cycles are done. User's editor has not been killed/restarted.

## Reviewed checkpoint after tail-shoe correction

- All four corrected v3 images were edited with built-in imagegen, saved under `references/transitions`, and copied to `tmp/image-previews/chapters-polish-2026-09-12/`. Provenance and the canonical anatomy brief are in `references/tail-foot-correction.json`. The first movie batch is superseded. The second batch uses two legs plus a shoe-wearing tail in its actual saved Wan workflows.
- Selected HighwayA-v3 and RestStopB-v3; rejected HighwayB-v3 because its tail shoe disappears around2seconds, and retained RestStopA-v3 as an alternate. Selected121-frame/24fps5.04-second MP4s and posters are staged at `Assets/JH/UI/ChapterTransitions/`. No scene binding has occurred. `review/accepted-exports.json` records hashes and acceptance boundaries. Wan PID34576 was stopped only after verifying its owner command line and empty queue; `wan-shutdown.json` records cleanup.
- All nine mascot rigs completed and passed standard fresh GLB mesh gates. Front/back and walking/attack evidence was reviewed. Corrected `evidence-fresh-grounded-*` renders use source30fps and exclude the glTF importer's Icosphere bone-display helper from bounds. Prior evidence incorrectly placed the floor at-1m. GLB minima are within0.000001m; actual FBX checks found simplification lift up to0.0046m, so `reexport-mascot-fbx.py` writes `-full-keys.fbx`. Those remove the lift; up to0.008m walk/run penetration remains in Blender FBX reimport and is explicitly pending Unity validation.
- Final prop set is eight, not seven. Sound barrier64's white slab was around18percent of its height, above a malformed lower protrusion, so the low-band heuristic missed it. A visually verified planar cut produced `props-v5/64`. Guardrail49's disconnected low scraps inflated depth to1.92m; v5 removes91fragment vertices and gives0.61m depth with physical bases intact. Other six repairs retain v3. All eight pass fresh mesh gates. `ChapterPropRefinement` is prepared but unexecuted.
- `ChapterMascotImporter` preserves stable Highway combat paths and authors three RestStop roles; body/equipment material slots come from the fresh pose metadata. The final import source is the full-key FBX. `RewardedAdsService` now has idle-only maintenance/no-fill retry and a45-second stale-load timeout, preventing a disabled ad button from remaining unavailable indefinitely after no-fill. Source is compiled; native UI/SDK behavior is still pending.
- Offline Editor/runtime compilation with `AdditionalRuntime.targets` passed with zero errors and16existing warnings; repository harness validation passed. A headless ce-compound learning was written and its frontmatter validated. No current-task workbook rebalance, standaloneRestStopscene, skin-shop live review or actual three-pass gameplay verification has been completed. Resume from the checkpoint README after Editor recovery; preserve fresh user state first.

### Final checkpoint checks

- After all owned generation processes ended, `unity pipeline list` still discovered the running Editor, but `unity command --project-path . list_open_scenes` timed out after30000ms. The block persists without the generation workload. The Editor was preserved.
- Final incremental Editor/runtime build passed with zero errors/zero newly emitted warnings. Python syntax compilation, the solution frontmatter validator and the focused tracked-source whitespace check passed (Git only noted normal LF-to-CRLF conversion). The accepted export manifest was regenerated and both staged movie hashes verified.
- The ce-compound headless workflow wrote one new learning, linked it from the existing documentation index, and found that AGENTS.md already explains how/when to search categorized solution metadata. No instruction-file change or blocking approval was needed. Existing related mesh/runtime/testing learnings remain applicable; no automatic refresh is recommended.

## Resumed continuation — remaining chapters and verification

- User asked to continue the remaining work. Original Editor10140 still does not service commands. A newly discovered `node_repl`/Computer Use surface was tried according to its skill; `@oai/sky` initialized but `list_apps` reported native-pipe unavailable (os error2). User asked asynchronously whether a save/confirmation/test window is present; no reply yet. Do not use guessed Windows UI input or kill the unsaved original Editor.
- Prepared `RestStopChapterBuilder` and `reststop-layout.json`:2100m route with entry parking, shops, outdoor dining, charging, service/delivery and exit garden zones.50enemies (30ranged),25bonus pairs and25gimmick stations; minimum beat spacing22.2m,35m corner exclusion and gates remain on the same route segment. This is authoring source, not yet a saved playable scene.
- Added RestStop scene navigation/palette selection and runtime placement registration. RestStop uses three new roles plus shared DeliveryRider/TireBruiser. Read the actual projectile contract: Guard-based DeliveryRider is ranged; ParkingMarshal now uses the sword combat base to remain melee. Added `HighwayFormationRefinement` so successive pairs leave alternating side gaps instead of an always-empty center; matching workbook patrol span is1.2m.
- Added `RuntimePerformanceBudget`: target60fps (15when unfocused; batch tests stay60), preserve desktop VSync, cap physics catch-up to0.1seconds, restore Editor settings on destruction. These are contention mitigations, not a proven repair for the original Editor stall. `ChapterPresentationInstaller` binds selected chapter movies, workshop story/defeat presentation, optional rewarded button and privacy options; test-ad settings are prepared. A duplicate privacy button listener was removed during review.
- Prepared `outputs/chapters-polish-2026-09-12/balance/Data.xlsx` as an initial calibration candidate. Six affected sheets are grafted with shared deduplicating styles; unrelated archive parts remain byte-for-byte unchanged and styles remain about10KB. Core ATT/HP extend to60levels; chapter targets20/40/60/80/100, Highway/RestStop coin rewards30/70, chapter2/3 combat and hazard values, later regeneration and100RestStop rows are included. Cosmetics' existing moderate effects and special-helper contracts were reviewed without forcing arbitrary changes. Canonical source remains the6cf790... workbook until native installation/testing.
- Artifact Tool rendered before/after views, found no formula errors and passed three input-change scenarios (RestStop attack linkage, independent chapter coin rewards, first/late upgrade prices). Actual shared `GameDataWorkbookSchema.Validate` and `EncounterPlacementTables.Read` also passed in an isolated .NET AssemblyLoadContext with Unity logging disabled:300placement rows,50/25/25 per scene. The first default-context attempt had aCodePages9/10 binding conflict; isolation resolved it. This checks real parsers but does not replace Play Mode.
- Preference helpers now support fresh task-specific snapshots and include `ads_last_reward_round`; the progression harness accepts that explicit snapshot path and recognizes RestStop. No fresh user snapshot has been taken or restored yet. Opening captions now explicitly say two feet and a tail shoe.

### Separate native Unity QA project

- A real-file copy of Assets(about11GB), ProjectSettings, Packages, GooglePackages and immutable PackageCache was made inside `tmp/q`; it shares no writable Assets or Library databases. Source-scene/prefab/data hashes are in `qa-copy.json` for later conflict checks. The first longer path hit Windows path limits; the partial copy was moved within verified workspace paths, resumed using extended-length paths and completed. No original asset was removed.
- The retained legacy HTTP connector was excluded only in the QA copy (its embedded folder moved to QA tmp). Supported control remains official Pipeline. A QA-only bootstrap changes company/product to `TralaleroQA`/`Chapters_20260913` and disables analytics before Play so tests use separate local preferences. That bootstrap must never be merged into production.
- QA EditorPID32616 was launched resident in batch mode with graphics through the documented Unity CLI workflow, hidden window, CPU affinity255/eight logical processors and below-normal priority. Owner record `qa-editor-owner.json`; log `tmp/q/qa-editor.log`. Pipeline discovery sees both original and QA instances. At last observation QA was still importing meshes and `qa-ready.json` was absent. Always address QA with `unity command --project-path tmp/q`; the configured MCP server is still pinned to the original project.
- Prepared `tools/install-chapter-balance.cs`, `chapter-polish-import.cs`, snapshot/restore scripts in both roots. Wait for QA bootstrap/compilation, then use detached long commands and asynchronous tests. Validate/merge only intended generated artifacts with source-hash checks. Preserve the original unsaved Noryangjin scene; do not overwrite it with the QA scene without reconciling its live unsaved work.

### QA native integration and first runtime checks

- QA bootstrap completed and official commands work. Verified company/product `TralaleroQA`/`Chapters_20260913`, batch mode and QA data path. Took58-key QA preference snapshot. Installed candidate hash56cf87420ec392fc3a0b49205646bd3f7e783bf933fc82ff717e5fb0015dffe7 and generated/round-trip-verified its signed runtime archive. Original workbook and original Editor are still untouched by these QA mutations.
- Pipeline eval's reference set contains two Newtonsoft JObject definitions (one embedded by Localization). The first install snippet did not compile and changed nothing; its small recorded-hash reader now avoids that ambiguous type. Long scene/render mutations must use detached jobs: eval_file defaults to5seconds internally, and a transport timeout does not undo work that already started.
- First native import exposed FBX conversion transforms: static prop roots carry100x scale. Resetting them toone made them too small; preserve imported rotation/scale. Rig bones also carry100x scale, so hand child offsets/scales must be converted to world dimensions. Corrected projectile socket world offsets/inverse scale, normalized inherited combat prefab roots, and removed duplicate coffee visual children on rebuild. Second import succeeded for9enemies/8props and official test-ad settings; native reports are in QA `map-concepts/chapters-polish-2026-09-12/mascot-import.json` and `prop-import.json`.
- Created QA `RestStop.unity`:2100m,110road tiles,50enemies,25bonus pairs,25gimmick stations,126top-level scenery/trigger objects. Authored alternating Highway formation gaps. Scene UI first failed because the Highway opening starts inactive; all authoring lookups now include inactive children. RestStop also starts with its opening inactive, retaining the dedicated arrival transition. All three scene presentations were saved; the last synchronous UI call timed out at5seconds but inspection confirmed completion and a clean Noryangjin scene.
-55unique related Unity EditMode cases passed:4new chapter presentation/road-support cases plus51equipment/Highway/placement/timing/reward/map-tool/workbook cases. Results are under QA `map-concepts/chapters-polish-2026-09-12/tests/`. The async test collector tolerates the status file's delete/rewrite window and resumes already-started suites instead of duplicating them.
- Entered actual QA Play Mode. `ChapterVisualProbe` renders the camera with a temporary screen-camera projection for root overlay canvases, restoring their original settings. First cold render exceeded eval's5-second budget; its logged error paused the Editor. Detached renders then completed in~0.1seconds. `capture-opening-delayed.cs` captures after video preparation/fade and pauses only the movie so the view remains inspectable.
- Runtime opening repeated on scene reload because these forward scenes do not contain GameManager and defeat uses the reload fallback. Added session-only auto-show tracking while preserving manual Story/Replay actions. Captured actual movie at0.4167seconds. Existing Noto variable SDF rendered thin; a temporary live Gmarket Sans comparison is much clearer. Both previews were copied to root `tmp/image-previews/chapters-polish-2026-09-12/`. Font-license choice remains pending; no claim of CC0 compliance. Asked whether the user permits the existing commercially usable font.
- Native ad startup revealed a real callback-order deadlock: consent callbacks were enqueued before MobileAdsEventExecutor existed. `RewardedAdsService.Awake` now initializes that queue before asking consent. A live pump recovery led to `Ready=true`, `Loading=false` and `광고 보고 보상 받기`; the fixed source was copied to QA and compiled. This is the SDK's Editor placeholder path, not an Android production ad test.
- First normal-stat calibration started through `tools/start-chapter-cycle1-nory.cs`:3runs, seed20260913, earned purchases, reset progress, fresh QA snapshot. Evidence folder is QA `tmp/image-previews/sr18-presentation-progression-2026-09-10/chapter-polish-cycle1-nory-20260913/`. Start job39a19e05eea04575bfee72f231f788c3; inspect runs.csv/conditions and harness status before proceeding. No three-cycle gameplay completion is claimed yet.

### Original recovery, native installation and progression calibration

- Original Editor10140 recovered without being killed. A duplicate launch failed and only that new process26352 was stopped; the precise reason the original resumed is unconfirmed. The original live scene and58 preference keys were backed up to `tmp/backups/chapters-polish-2026-09-13/original-*` before authoring. User wallet was3007coins/0gems. Native authoring preserved the original Noryangjin world instead of copying a QA scene over it.
- Nine mascot and eight prop imports succeeded in the original Editor. A separate RestStop scene is saved and registered,2100m/110road tiles,50enemies/25bonus pairs/25gimmick stations. Highway formations and all three chapter presentations are applied. Noryangjin→HighWay→RestStop use the selected5.04-second movies. RestStop has no invented chapter4destination.
- Refined RestStop scenery: continuous forecourts,24m decoration spacing, parking/store/dining/charging/service/exit zones, seating, stores and ambient people. Fixed correlated parking selection that had excluded red cars. QA refinement reports73scenery blocks/63ambient animators/49occluder groups; original scene creation uses the same revised builder.
- Found a primary-source CC0 Korean family: Jigmo, https://kamichikoichi.github.io/jigmo/. The20250912archive license explicitly grantsCC0-1.0. `prepare-cc0-game-font.py` subsets/renames it Jigmo Game UI, retaining13,344characters including all11,172modern Hangul syllables. Source/license/provenance are shipped. Native TMP dynamic SDF includes216prewarmed characters in one2048atlas; all212original Noryangjin Canvas labels now reference it. First asset creation lost material/texture references and was repaired by retaining/reattaching subassets. Repeated TryAddCharacters return values were misleading; validate HasCharacter instead. The prior Gmarket preview is superseded, not the shipped font.
- Cycle1 ran three ordinary-stat attempts per scene. Noryangjin29.41/44.10/53.94seconds. Diagnostic Highway(HP960/ATT48)109.84/109.77/167.85seconds; diagnostic RestStop(HP3300/ATT88)121.07/115.64/137.32seconds. The latter presets were diagnostic and not campaign carryover. Low kills starved later upgrades. Native projectile/capsule probes ruled out the suspected above-head shooting fault.
- Added bounded once-per-round progress coins: every15completed seconds gives30Highway/70RestStop coins, up to20checkpoints; Noryangjin retains its existing rewards. Reward is credited before analytics EndRun and shown on defeat. Seven additional native EditMode cases passed,62distinct related cases total so far. Removed per-shot and redundant per-second debug logging.
- Cycle2 carried actual earned progression. Noryangjin cleared on attempt22(HP960/ATT48,329remaining HP). Highway cleared on attempt8(HP1580/ATT60,1129remaining HP), too quickly for the~20target. RestStop is still being measured from that actual carryover. Candidate3 changes late core price coefficient3.4→3.1 and Highway first damage110→220, HP360→540, hazard damage180/240/280. This candidate is staged, not yet installed; original and QA still use cycle2hash98db9605b51cc9a0ad70261648681ea20b42cfdc9cb4de3852c423fff7e56ae5.
- Native test-ad settings are applied in both projects. Subsequent code review added a consent-update/SDK initialization timeout with stale-callback rejection, while deliberately imposing no reading timeout on an open consent form. Earned callbacks may credit a completed ad after the old defeat UI has been destroyed. These final changes still need QA compilation/live validation at this checkpoint.

### Final presentation, native animation and startup verification

- Latest ads/font/UI sources compile natively in QA and original. The ordinary dotnet Editor build also includes runtime and passed with0errors/2existing warnings. Repository harness validation passed. Updated chapter font assertions still await the final native test run.
- Native portrait UI review found a clipped second catalog row and thin body text. Reduced cosmetic card height310→280, strengthened story captions and defeat typography, and rebuilt all three original/QA scenes. The old `Canvas/UI/Main/Ads` image had no button and falsely promised20gems; it is hidden. Final opening/shop/defeat PNGs are copied to root `tmp/image-previews/chapters-polish-2026-09-12/*cc0-final.png`. Active result labels have no TMP overflow. First direct defeat capture included lobby overlays because it bypassed the gameplay flow; the superseding v2 test starts a run and uses the real defeat/reload path.
- SDK Editor placeholder:90coin offer paid1165→1255once; duplicate request paid nothing; fresh77coin offer closed immediately paid0and restored Continue. Detailed boundary/production steps are in `rewarded-ads.md`. Live mobile transport/no-fill/consent remains unverified.
- `verify-chapter-travel-live.cs` passed every recorded condition: natural movie completion automatically loadsHighWay; the second movie plays and Skip loadsRestStop; wallet, upgrades and equipment remain intact; opening does not repeat; no transition RenderTexture remains; chapter3unlocks. Evidence is QA `tmp/qa-proof/chapter-travel-final/result.json` and PNGs.
- Initial native BakeMesh pose inspection doubled FBX scale and falsely reported26cm lift. Two attempted correction bakes failed a guarded threshold, with no controller changed. The correct weighted bone-world/bindpose/vertex calculation at60fps verifies18bones/6actions for all9roles: living-action extrema within3.83mm, death interpolation up to14.47mm. Rejected baker and two unused clips were archived outsideAssets and deleted through AssetDatabase. The accepted manifest now links the native evidence and verifies all source hashes. The ce-compound learning records this correction; its validator passes with Python UTF-8 mode.
- Cycle3Noryangjin completed22attempts with actual ordinary upgrades: final startHP960/ATT52,299.99seconds,39.62remainingHP,34kills,25choices,320earned coins,bank458,ATTlv11/HPlv12. This supports~20attempts rather than an exact mandatory counter. Cycle3HighWay has begun from this actual carryover using candidateb06ee23... . Do not change QA Assets while it plays.
- Warm QA Editor startup measurement (`startup-profile.json`): first movie frame1.913seconds from Play request;480recordedframes,median16.68ms/p9518.13ms, initialization maximum766.41ms. This is a graphics-enabled Editor measurement, not a physical-device cold-start benchmark. A5-second normal-play process sample showed original5.59%CPU/QA5.73%CPU across20logical processors; no100%CPU freeze was reproduced in that sample.
- Read-back verified original58preference keys unchanged,3007coins/0gems, originalNoryangjin scene active. No preference restoration was needed and no QA product/bootstrap has been merged.

### Purchase-policy comparison and missing jewel economy

- Cycle3Highway's greedy-affordable policy was stopped only after recording attempt9:160.69seconds,HP1410/ATT60,4kills,450earned coins,bank711,ATTlv13/HPlv15. The policy repeatedly spent partial balances on attack speed while higher-value health was unaffordable. Added an optional `saveForBestUpgrade` comparison policy to the Editor-only harness; legacy behavior remains the default.
- A comparison replays the actual cycle3Noryangjin exit:458coins,ATT11/HP12/attack-speed6, other purchased levels0. ATT/HP/wallet come from runs.csv; the final145coin purchase uniquely identifies speedlevel6 in the actual table(first price45). A mistaken initiallevel8 assumption was blocked by a native price guard before any preferences changed. Two fresh snapshots preserve the greedy diagnostic and the reconstructed entry. `chapter-polish-cycle3-highway-saving-20260913` is running in QA on workbookb06ee23..., max26runs, normal movement and actual earned purchases. Verify its output before resuming; do not edit QA Assets during the cohort.
- Found that the existing jewel wallet had no runtime source: only the Editor cheat called/assigned it, and the old20-gem lobby image had no button. Drafted free chapter-clear jewel rewards so the skin shop is usable without ads: first20/25/30for the three chapters, replay5each. The once-per-run Completed guard and persistent `chapter_rewarded_<chapter>` key prevent duplicate first payouts. The first-flag is saved with the wallet; completed-entry movies and final victory UI show the reward. Preference helpers now include five additional reward keys.
- Candidate80b06a210353d0e680b3a161709b411bafab415e0d8924d965d2de9714cdaee9 adds only these jewel controls/environment formulas to b06ee23's combat/upgrade tuning. Artifact renders/formula checks/four input-change scenarios and the shared parser pass. It is staged, not installed in either project. Original remains98db9605..., QA remainsb06ee23... . New runtime/reward UI code compiles in original; its saved scenes have the reward label, but the new workbook and directed reward tests remain pending.
- `verify-chapter-rewards-live.cs` is prepared for QA after the new source/data are installed: first20/25/30, replay5, duplicate Complete calls, natural/skip movie transitions, coin/upgrade/equipment retention and visible victory reward. Run separately from progression and preserve/restore a fresh63-key directed-test snapshot.
- Added a narrow cosmetic scrollbar to retain scrolling affordance now that two full rows fit. Its latest source still needs native application/capture. `build-chapter-android-qa.cs` is prepared for an ARM64 Development/test-ad build in QA after all Play cohorts: switch target through official Pipeline and wait for reimport first. It never uses the original project for the build, and restores QA architecture/signing in finally. No Android build has started yet.

### Fourth Highway tuning pass started

- The saving-policy comparison cleared Highway on attempt14:300.13seconds, startHP1950/ATT64, finalHP2035.26after run bonuses,8kills/25choices,900earned,bank1286,ATTlv14/HPlv18/speedlv12. It improves the earlier8-attempt result but is still faster than the~20target.
- Raised Highway first attack220→300, health540→660 and obstacle damage180/240/280→230/300/350. All other combat/upgrade values, including RestStop, remain the prior candidate's. Final Highway boss1233attack/9735health. Candidatehash`ceffcb106af189e9a97789d6d3840527af8edfcd21e0f375c732b8952064eb40` also includes the new free-jewel economy. Artifact checks pass and native QA installation reports300rows plus verified signed archive.
- Latest runtime,63-key helper, reward UI and scrollbar source were copied to QA while stopped and compiled successfully. All three QA scenes were rebuilt with their reward labels and scrollbar. Original scenes are also updated with this UI; original workbook is still98db9605...pending final calibration/install.
- `start-chapter-cycle4-highway.cs` snapshots the14-run clear, restores the verified/reconstructed Noryangjin entry, credits the new20-jewel first-clear equivalent and flags chapter1rewarded, then starts `chapter-polish-cycle4-highway-20260913` with saving policy/max26. Entry is458coins,20gems,ATT11/HP12/speed6/default cosmetics. On completion use `start-chapter-cycle3-reststop.cs` to snapshot and continue actual carryover with saving policy. Do not change QA Assets during either cohort.

### Critical cadence correction — supersedes earlier ordinary-input certification

- Fourth Highway tuning failed to clear by26attempts(lastHP2360/ATT72,167.84seconds,bank1016). A measured test-harness defect was then isolated: over5seconds,248game frames received828Editor callbacks, up to7per frame. The old harness called PlayerMove on every Editor callback even though PlayerMove uses Time.deltaTime. Earlier progression counts remain diagnostics, not validated ordinary-input difficulty.
- `Sr18ProgressionPlaytest.Tick` now gates on Time.frameCount. Final CSVs append `movement_calls` and `game_frames`; actual completed rows show movement below the one-call-per-frame ceiling. The report generator rejects older/unbounded records. No production movement speed was changed.
- Reverted the excessive Highway attack/HP/obstacle increase to220/540 and180/240/280; retains3.1late-price coefficient and the free jewel economy. Current candidatehash`bfa60ed3ce9c2acd0a33182a8fbe992a82f75ba692021437c4c0898dcde11ee3` is installed/signed/round-trip-verified only in QA. Original remains98db9605...until final acceptance. Candidatebfa is semantically the earlier80b06reward revision; the ZIP hash differs on regeneration.
- New final chain uses `start-chapter-final-nory.cs`, then `...-highway.cs`, then `...-reststop.cs`, max30each, saving policy, default cosmetics, normal PlayerMove at3xsimulation speed. Nory resets earned progression and first-clear flags; subsequent stages snapshot and carry real state without reconstruction. The running folder is `chapter-polish-final-nory-20260913`; first10runs are recorded at this checkpoint. Use only the `chapter-polish-final-*` cohorts for final ordinary-input claims. Older cycle3RestStop has not run and its start script is superseded.
- Ads review found a form-timing race. Consent-form loading is now separate from showing; a loaded form waits until gameplay/opening/chapter travel are idle. Only an active native form blocks the public start handler; network preparation does not hold gameplay for60seconds. Prepare/Show/privacy actions respect gameplay/story activity. Latest source compiles and was copied to QA before the final chain, but final native ad/form behavior still needs verification.
- Updated original `ChapterPolishIntegrationTests` passed4/4 in2.52seconds after save_all/asynchronous execution, includingCC0font and new reward-label references. Result is `tests/ChapterPolishIntegrationTests-final-main.json`. The previously62distinct related cases remain the broader native coverage count, not66unique tests.
- Added/validated the frame-cadence learning in the existing runtime/progression solution document using ce-compound headless. Temporary matplotlib3.11.2 dependencies were installed only under`tmp/chapter-plot-deps`; `report-chapter-progression.py` uses the bundled Python3.12 interpreter, requires final frame-bounded CSVs and saves a non-overwriting standalone progression PNG. Run it after all three final cohorts clear.

### Frame-corrected Noryangjin clear and Highway combat-window correction

- `chapter-polish-final-nory-20260913` cleared on attempt17:300.00seconds,startHP960/ATT44,finalHP532.92,34kills/25choices,320earned coins,bank388. Input4097calls across5966game frames; all completed rows respect the one-call ceiling. First-clear20jewels were actually paid. `final-highway-entry.tsv` is a fresh63-key snapshot with388coins/20gems,ATT9/HP12/speed3,chapter1rewarded and chapter2unlocked.
- The initial frame-corrected Highway cohort was stopped after29recorded attempts(last1950HP/68ATT,167.83seconds,bank885). Inspection found another harness assumption: avoiding enemies from22m cut off shooting before projectiles had enough time to reach them. The final approach window now avoids surviving enemies at8m; the existing14m prop and11m projectile warning handling is unchanged. This only changes Editor test strategy, not game damage/movement.
- `chapter-polish-final-highway-combat-20260913` now replays the actual saved Highway entry (no inferred levels), with frame gating, saving policy and8m enemy avoidance. Source and signed workbook remainbfa60ed3.... After this clears, stop Play and run `tools/start-chapter-final-reststop.cs` for the real carried state. Do not use the earlier `start-chapter-cycle3-reststop.cs`.
- Prepared final directed helpers: `prepare-final-directed-tests.cs` takes63keys, opens Noryangjin and enters1xPlay; `verify-chapter-ui-live.cs` now writes-v3evidence and verifies the scrollbar; ad earned/early-close scripts also write-v3and wait for the callback queue before checking closed state. `verify-consent-start-guard.cs` injects only form/loading state while exercising the actual public start handler. Run the reward/travel script from a fresh Noryangjin Play session, then stop and restore `final-directed-tests.tsv`. None of these directed wins count as ordinary progression.

### Approach geometry and chapter-income correction

-8m avoidance was too late for keyboard steering against moving formations; that diagnostic ended at24runs. Read-back measured sensitivity1/divisor125/smoothness25, player collision width1.38m and lane range±4.4m. The harness now aims at the inner enemy beside the alternating safe lane, uses14m enemy avoidance, and caps moving-road-enemy predictive padding at0.6m instead of inflating it by up to2.1m. Noryangjin behavior is unchanged. Three approach trials produced28.47/28.46/39.97seconds with actual kills.
- Those trials exposed a game economy/collection issue: at higher permanent-upgrade prices, only30–60coins arrived, and a0.6m coin trigger missed kills when the player moved into a safe neighboring lane. Added optional scene-configured planar coin collection with a2.5m vertical separation guard and one-shot collection. Original coin colliders are unchanged, avoiding additional projectile obstruction. Noryangjin has no new radius override; Highway/RestStop use3.2m through workbook keys.
- Scaled chapter2/3 income with later upgrade costs: Highway enemy/progress90, RestStop210; progress still counts each15completed seconds, max20checkpoints. Future chapter4/5 design coin rows become420/720. Combat remains220/540Highway and260/1100RestStop. Current candidatehash`f8d5b8fb79118664e77be704e90d585a5f2c51cea75fe137b7413b6a77c0c4f6` is native-installed and signed/verified in QA; original remains98db9605.... Artifact checks passed; final shared-parser/report refresh is still required.
- Current accepted Highway candidate cohort is `chapter-polish-final-highway-economy-20260913`, restored from the actual63-key Highway entry. First3runs:28.48/28.47/40.00seconds, earnings180/180/270; third run boughtHP13using real coins. Input calls384/385/594against566/567/797game frames. The three-run diagnostic was resumed in place to max30via `resume-current-cohort.cs`; no progress reset or added coins. Continue monitoring this folder, then carry its real clear into `start-chapter-final-reststop.cs`.
- `verify-coin-collection-qa.cs` is prepared as a separate directed check: collect37coins at3m, leave6m/different-floor pickups intact, and verify exactly one credit. Run from fresh Highway Play inside the directed-test snapshot boundary. Latest MoneyScript is compiled/copied to QA. Updated report generator selects finalNory + HighwayEconomy + finalRestStop.

### Verified Highway pacing and final RestStop calibration

- Highway's90-coin income version cleared in27runs,300.13seconds,startHP4110/ATT100,finalHP1,24kills/25choices,4500earned,bank5221. Income was then increased to150enemy/progressHighway and350RestStop to better match later purchase costs; future4/5design coins700/1200. Combat/input rules stayed unchanged. Candidate90755340d9da8b3b87b40b375e18efb2a2bd1b74ead08f0663b6812ad110de3e passed native install/signature checks in QA.
- `chapter-polish-final-highway-paced-20260913` replayed the actual saved Noryangjin entry and cleared on attempt22:300.15seconds,startHP5660/ATT116,finalHP7825.47after run bonuses,27kills/25choices,8850earned,bank11582,ATT27/HP32/speed30.3995movement calls/5958gameframes. This is the final accepted Highway pacing evidence. New63-key `final-reststop-entry.tsv` preserves actual11582coins/45gems and those levels.
- Initial finalRestStop(attack260/HP1100) was too easy after the real Highway carryover:197.97/279.68/279.72seconds, then a4th-run299.89clear atHP7850/ATT136. The late-wave health was a collision cost as well as shooting durability: `EnemyScript_space.ResolvePlayerContact` subtracts remaining enemy health, whereas `_damage` drives ranged projectiles. This rule is preserved and must be considered when changing enemy HP.
- RestStop was adjusted to first attack650/HP2750, matching the preceding Highway late regular-enemy scale; hazards500/650/750,breakHP1250. Growth/tier multipliers stay. Last RestStop boss2671attack/28950HP. Candidatehash`f4fa92268135bf3fd84ae8191cda92ddb85dcdc7e0ed5a350e6e0c4102660e3e` is installed/signed in QA only. Original remains98db9605... . Source/data rendering checks passed; final shared-parser report refresh pending.
- Current running cohort: `chapter-polish-final-reststop-paced-20260913`, initially max3, actual restored Highway exit, same14m/inner-enemy/frame-bound steering and saving policy. First run86.26seconds,1750coins. After reviewing three runs, resume the same cohort with `tools/resume-current-cohort.cs` if progression is reasonable. Do not edit QA Assets during the run. Final report generator now selects finalNory + finalHighwayPaced + finalRestStopPaced.

### Last RestStop income pass running

- RestStop's corrected combat with350enemy/progress coins cleared on attempt13:299.90seconds,startHP9080/ATT144,finalHP230.78,5kills/25choices,8750earned,bank11013,ATT34/HPl41.4271movement calls/5980gameframes. Combat is proven completable but the income made progression faster than the~20target.
- Reduced only RestStop enemy/progress coins350→175. First/replay jewels30/5, all combat/hazards and Highway150income remain unchanged. Current candidatehash`d8f7e34b78c0979e2fa3e16efd9b68ce52ec50dd6d97923a62092fc10cb646dc` is installed/signed/verified in QA. Original still has98db9605...until final data installation.
- Current running cohort is `chapter-polish-final-reststop-economy-20260913`, max30, restored from real `final-reststop-entry.tsv`. First2runs106.13/106.05seconds,1225coins each; first5660HP/116ATT, second6710HP/120ATT after11551real coins spent from the carried bank. `tools/report-chapter-progression.py` now uses this cohort for the final RestStop result.
- After this completes: stop Play; run the prepared63-key directed snapshot/UI-v3/ad/consent/coin/reward-travel checks; restore directed preferences; run final related native suites using `run-chapter-editor-tests.py --run final --suite ...`; generate the progression report with bundled Python3.12 plus the local plotting dependency folder; install/verify final workbook in original; refresh relevant docs/accepted evidence and original preference checks; then perform the QA-only Android test build and stop only the owned QA Editor. No mobile build has started yet.

### Final accepted pacing and functional acceptance

-175RestStop income did not clear by29runs.250income cleared at32runs(299.94seconds,HP12800/ATT168,final4756.94,bank6578). Final300income was selected between the fast350and slow250observations, restored from the actual Highway entry and fully replayed.
- Final `chapter-polish-final-reststop-confirmed-20260913` cleared at22runs:299.93seconds,startHP11330/ATT156,finalHP176.10,8kills/25choices,8700earned,bank8913,ATT37/HPl46.4244movement calls/5980gameframes. Final accepted chain is17/22/22; the plot and `progression-result.json` use only these frame-bounded, actual-carryover cohorts.
- Final workbook/source hashc233f05baec8f91ae8ab4b9437ce06f11509068914a06bbbaad7fb6add43988b is installed and signed/round-trip-verified in original and QA. Highway enemy/progress150,RestStop300; combat remains220/540Highway,650/2750RestStop. First/replay jewels and collection radius stay. Original58keys remained unchanged3007coins/0gems, and the new five reward keys are absent in original.
- Final native UI-v3/ads checks:6578→6668for90earned once; duplicate ignored; early close gives0and Continue becomes available. Simulated form state blocks the actual start handler while loading alone permits start. Coin test:37at3mcollected once,6m/other-floor coins remain. First20/25/30and repeat5jewels, duplicate completion, natural/skip travel, coin/upgrade/equipment retention and movie target cleanup all passed. `final-checks/` retains results. Directed63-key snapshot restored after each check.
- Victory's old blue panel was replaced with the workshop card style in all original/QA scenes. A first queued screenshot captured the subsequent lobby; v2held the screen. A separate capture was mistakenly invoked from an earlier auto-travel chapter and its unguarded Editor callback kept throwing, blocking later Pipeline handlers. Only the saved, owned QA Editor32616was stopped after PID/path/start-time verification. QA restarted hidden asPID5164with the same CPU bounds; original10140was preserved. Corrected capture checks RestStop, unsubscribes on timeout/Play exit/destroyed target, and produced the verified final victory PNG. Owner record was updated; old owner retained.
- The final UI screenshots and progression graph are under root `tmp/image-previews/chapters-polish-2026-09-12/`. The graph uses Jigmo at its real weight; a missing middle-dot warning was removed with supported separators. Runtime reward text likewise uses plain spacing. These text-only source changes are copied/compiled for Android.
- Final43native related cases passed. Android attempt1was blocked by the old Firebase build validator expectingEDM1.2.186despite the new pinned1.2.187required by ads. The installed1.2.187archive was rechecked against registry SHA512; SHA256is0E0D6A1A088E82B2CAD8CACFAD5DE768D849602E035F753AEB621114709F5D40. Updated the exact version/hash validator, its missing-package test and SDK documentation. Its40native tests passed; final suite count83and total distinct related cases102. Exact pins/destination checks remain enforced.
- QA platform switchStandaloneWindows64→Android completed after texture reimport. The build runs from a one-shot delayCall with settings restoration in finally, avoiding eval's short execution budget. Attempt1failure/result are retained; attempt2is currently running under QA `android-build/attempt-2/` with ARM64/debug signing/test ads and explicit three-scene list. Original signing settings were not changed. Read `result.json`/`error.txt`and restore marker before calling the Android build complete.

### Completed Android verification and cleanup

- Attempt2finished shader/IL2CPP work but failed Android manifest merge: project minimumAPI23was below the pinned Google Mobile Ads wrapper'sAPI24requirement. Increased original and QA minimum toAndroid7.0/API24and added an early `RewardedAdsBuildValidator` check. No overrideLibrary suppression was used. Android6.0support is excluded and documented.
- Attempt3succeeded with0errors/44warnings,87.55seconds using the compiled cache. Explicit three-scene ARM64Development/test-ad APK is copied to `Builds/Android/ChapterPolish-Test-20260913.apk`,727656474bytes, SHA256adcb1da267d392e723ba22103fbeb731893e9d4c3772d9948a448f5292d30010. BuildSummary's3124086107bytes is not the APK file size. AAPT verifies minimum24,target36,arm64-v8a; apksigner verifies v2signature/one signer. This is not a production-signed/store release or physical-device test.
- Exact EDM1.2.187archive hash and minimumAPI24checks are in original source. Native Firebase preparation40tests and final43related tests all passed(83final;102distinct across revision). Final normal C#build passed with0errors/0newly emitted warnings. Metadata/frontmatter/harness/source-format checks passed.
- Final acceptance,source/runtime hashes,APKhash and preserved preference state are in `final-verification.json`; native functional/build records in `final-checks/`. Latest previews include `opening-final-v3.png`,`equipment-final-v3.png`,`skins-final-v3.png`,`defeat-final-v3.png`,`victory-final.png`,`reststop-arrival-final.png`,`progression-verified-v2.png`. Docs use the actual17/22/22final cohorts and retain prior failed/biased diagnostics.
- QA's final63-key state was saved; ARM64/custom signing restored,minimum24recorded. Owned QA Editor5164exited through the official Editor API after PID/path/start-time verification. OS read-back confirms it stopped and original10140remains running. Original58keys remain unchanged3007coins/0gems, all five new reward flags absent, originalNoryangjin scene active. No QA product/bootstrap or preference state was merged into original.
- No commit,push,publishing,message to others,production AdMob activation or invented chapter4/5scene was performed. User action remaining for real monetization is supplying real AdMob IDs/audience configuration and a device test; the requested test-ad setup and current three-chapter implementation are complete.
