# Enemy capsule radius 0.43 → 0.55

Status: complete in project assets. No new APK build/install in this follow-up.

Used native Unity authoring via `tools/set-enemy-radius-055.cs`, targeting enemy-owned root capsule colliders only. Checked the three gameplay chapters and 235 source prefabs in the project enemy/prefab folders. SR18 contained 38 remaining matching scene capsules; six already had 0.55. Changed those 38 and four matching source prefab capsules. SR18 now has 44 at 0.55, four at 0.87 and two at 0.82 (including inactive placements).

For affected `EnemyHitboxSize` components, stored baseline radius is `0.55 / 1.3` so the existing 30% initializer produces exactly 0.55. Native verification invokes initialization and repeated `Configure` calls and checks that radius stays 0.55 while height and center are preserved. No runtime algorithm change.

All three chapter and source prefab audits report no remaining enemy root capsule with radius 0.43. Other radii are retained. Before images and a per-collider change manifest: `tmp/enemy-radius-055-2026-09-21/162923/`.
