# Harbor opening and UI refinement

Status: complete. Implementation saved, final verification passed, 35 reviewed PNGs collected, and all 77 tracked preference records restored without mismatches (coin 191, jewel 0). SR18 is open in Edit Mode.

This is the user's11-point2026-09-16 refinement request, including the attached directional pier screenshot. Shared UI and enemy-pair health apply across the three chapters; the dock/shop/signal/early-bucket staging concerns Noryangjin.

Progress, decisions, test results, generation provenance and before/after views are recorded here during execution. Preserve previous Coastal UI work and current user changes.

## Current implementation

- Fresh baseline saved the user's dirty SR18 scene through Unity before changes; source/Data.xlsx/Data.bytes copies and preference snapshots are in `tmp/backups/harbor-opening-refinement-2026-09-16/`.
- Official CLI works against the new Editor PID; the previously connected MCP instance timed out. Use `unity command --project-path . run_script --file ... --entry ... --timeout_ms ...` for long authoring and `eval_file --file` for short/coroutine scripts. Dynamic run_script object arrays do not reliably serialize; use explicit TSV files for probes.
- Existing project Gmarket Sans Bold (SIL OFL, official https://corp.gmarket.com/fonts/) is now a dynamic SDF (`GmarketHarbor SDF.asset`). Original font license metadata is retained in `font-license.json` and StreamingAssets/ThirdPartyNotices. New imports were not required for the font; the official ZIP was fetched to verify provenance. Presets now copy atlas/gradient properties from the selected font and remove synthetic face dilation; light text keeps a restrained outline.
- `HarborGameUIInstaller.Refinement.cs` refines all three chapters' typography, shrinks HUD, restructures settings rows/sliders, and makes tutorials a6.5%-height top toast. Previous Scene replaces replay in video UI; `OpeningStoryUI.Previous()` seeks to the preceding real movie boundary and disables at scene0.
- Workbook artifact-tool authored394targeted edits.75right-side enemies have `ROUND(left HP*(1+15%),0)`; activation distances differ across pairs, first left old man approaches2m, and first SR18 bucket row is disabled. XML graft preserves all parts except sheets9/11. Signed runtime archive was regenerated by GameDataWorkbookEditor.
- Opening: five straight pier modules add 56.25 m; spawn moves from (27.09,.12,-115.11) to (75.09,.12,-115.11). Final gantry is at (18,.02,-115.11), rotated across the straight and enlarged 30%. First left old man is 4 m farther along the outgoing segment, at (-11.84,.12,-82.75), and walks 2 m toward the player. Workbook lead 12 preserves the existing corner/trigger clearances. Floor sampling uses the same small-offset tolerance as placement validation to avoid rejecting plank bevels.
- Root cause of identical idle waves: OldMan and net-man idle overrides used their act clip. New `_Calm` overrides hold relaxed arms with subtle breathing/head motion, and idle phase is staggered by placement name. Applied to three scenes.
-150bonus altars plus Box_left/Box_right source prefabs have lowered `PickupPedestal`, readable SDF outlines, a Bonus label and distance-bounded Cartoon FX glow. Successful claims explicitly spawn a bounded pickup burst. Prefabs are authored before scene overrides to avoid duplicate added components.
- New TRELLIS assets: workshop24,704tris, merchant source23,484tris, retained under `outputs/harbor-opening-refinement-2026-09-16/trellis/`. Task-owned idle backend was shut down after both jobs completed.
- Final workshop at (24,.05,-121.8), yaw 60 degrees, on the connected side deck. Merchant at (28,.05,-119.3), eighteen-bone rig with SeatedIdle/StandGreet and a separate supported stool. The signal is farther along the path so its post does not cover the merchant. Rig v4 narrows the pelvis weight region to exclude the glove; previous failed poses remain in the evidence folder. Final Blender/FBX/GLB are in `outputs/harbor-opening-refinement-2026-09-16/merchant-rigged/` and installed under `Resources/HarborRefinement/Merchant`.
- Mobile SDF outline parameters needed the `OUTLINE_ON` shader variant; this is now enabled for white/bonus/enemy labels. `HarborWorldTypographyInstaller` updates 166 enemy labels and 15 source prefabs, plus remaining scenic labels. Highway direction signs now fit two separate rows. `GameUIFont` supplies the same family to runtime damage text. Screen and player-following health fills use teal instead of saturated green.
- `GameAudioService` tolerates focus callbacks during partially destroyed audio channels. A focused native lifecycle test covers this failure.

## Verification

User review folder: `tmp/image-previews/harbor-ui-all-2026-09-15/4-refined-2026-09-16/index.html`. Original 1/2/3 concept folders remain intact. This collection contains 32 native screenshots and 3 separate model validation renders.

- 62 focused native tests passed: HarborRefinement 10, EnemyEventController 24, CoastalUIIntegration 9, PlayerStatusHud 7, BonusWallChoicePair 6, BonusWallCooldown 6. The final expanded font test verifies every saved scene TMP label and runtime `GameUIFont`; its result is `tests-release.json`. Earlier `tests-final.json` retained a managed-wrapper SameAs assertion failure; the corrected assertion compares verified Unity native texture identity.
- `runtime-health-*.tsv`: 25 pairs per scene, all 75 actual left/right HP values match the protected workbook archive. The first pair is 45/52 after integer rounding (15.56%).
- `opening-runtime-final/checks.txt`: natural traversal from the extended pier through the first corner, minimum player Y .06899; first actor walks while second waits; actual second-gate collider starts its attack; glow activates nearby; actual pickup increases attack 68 -> 75.34281, removes both choices and spawns exactly one burst.
- `release/<scene>/checks.txt`: three scenes complete real Previous page 2 -> 1 playback, actual options/upgrades/chapter/shop/pause activation and screenshots. SR18 also verifies departure greeting and disabled first bucket. `final-gameplay/` contains later gameplay views after the final scenic-sign font corrections.
- `merchant-glb-final-metrics.json`: fresh-import technical inspection. Seated/waving views retained in `merchant-seated-v4/` and `merchant-greeting-v4/`; workshop fresh multiviews in `workshop-fresh/`.
- Runtime and Editor dotnet builds pass; `harness-validation.txt` passes. Final managed code build reports zero warnings; an earlier clean build reported only the pre-existing SplineSpeed unused-field warning.
- A final native test initially waited behind Unity's modified RestStop scene prompt. Saving task-owned pending scene state resumed the tests successfully. Use `SaveOpenScenes` before initiating test runner transitions and inspect modal windows if Pipeline stays reachable but main-thread commands time out.

## Recovery and scope

- `tools/restore-harbor-refinement-state.cs` restores ONLY this task's `playerprefs.tsv` and `extra-prefs.tsv`, verifies typed values, restores the exception trace setting, and returns to SR18 Edit Mode. See `preference-restoration.txt` for the executed result. The Game View dimensions were retained.
- Existing concepts, failed candidates and pre-task code/scenes/workbook are preserved. No unrelated package bulk import, APK build/deployment or remote publication occurred.
- Acceptance is Unity Editor visual/functional acceptance. Device frame rate, full normal-health chapter completion and subjective human art acceptance remain separate checks.
- Reusable corrections were captured by `ce-compound mode:headless` in `docs/solutions/ui-bugs/verify-mobile-sdf-and-world-presentation-in-native-frames-2026-09-16.md`. Existing AGENTS.md already exposes the solutions directory; no instruction-file edit was needed.
