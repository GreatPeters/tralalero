# SR18 실제 맵 전투·BonusWall 적용

상태: **2026-09-06 실제 SR18 씬에 적용·저장 완료.** 그림용 복제 맵이나 확대 배치를 저장한 것이 아니다. 현재 `map2` 브랜치의 `Assets/ShooterSurvival/Scenes/Tools/Noryangjin_MapTool_Mode_SR18.unity`를 사용한다.

## 적용 내용

- 원본 길 230개, 기존 Props 666개, 물 1개, 코너 15개와 경사 스팟 8개 및 게임 인프라 보존.
- `Enemies`: 기존 6종 프리팹으로 25개 편성, 적 69명. 일반 63명·엘리트 5명·보스 1명.
- `Bonuses`: 9지점에 14개. Normal 6개·Rare(엘리트) 7개·Unique 1개. `RuntimeBonusWall`의 맵 배치 보존 플래그를 설정했다.
- `Props`: 연결된 적 발동 스팟 31개 추가. 현재 직접 자식 총 697개. 순차 편성 E02/E10/E12/E18/E21/E24는 두 번으로 나눠 발동한다.
- `SR18_EnemyTargets`: 이동 적 27명의 독립적인 목표. 일반 칼 적은 플레이어 쪽으로 4유닛 이동 후 공격하고, 첫 고가 위 E15는 제자리 공격한다. E20 뚱보만 이동 후 공격하며 나머지 뚱보와 경비원은 발사한다.
- 표준 맵툴 크기: 일반 적 2.25, 여성 보스 2.5, BonusWall 약 3. 그림용 추가 2.5배 확대는 사용하지 않았다.
- 발동 스팟 월드 크기 8.8 × 2 × 1.2, 편성 앞 약 18유닛. 짧은 입구는 코너 뒤 여유를 우선한다. 쌍 보너스 중심 간격은 4.4유닛이다.
- 첫 고가의 E14/E15와 발동 스팟은 높이 약 12의 상부 평지에, F05는 경사를 내려온 평지에 배치했다.

## 실제 화면

- [저장된 맵 전체](../../tmp/image-previews/sr18-encounters-applied-2026-09-06/03-installed-map-overview.png): 실제 씬 렌더. 그림용 오브젝트·확대 없음. 판독을 위해 이 촬영 카메라에서만 UI 레이어와 그림자를 제외했다.
- [플레이 모드 시작 화면](../../tmp/image-previews/sr18-encounters-applied-2026-09-06/01-game-start.png): 실제 캐릭터와 첫 적.

중간 SceneView 캡처 `02-actual-scene.png`는 현재 작업 영역 확대와 화면 UI가 겹쳐 어둡게 나온 참고 파일이며 전체 비교용으로 권장하지 않는다.

## 기록과 재현

- [적·보너스·스팟 실제 좌표](placement-report.json)
- [플레이 모드 발동·이동·리셋 검사](runtime-probe.json)
- 원본 기획: `../sr18-full-enemy-bonus-flow-2026-09-06/full-flow-final.json`.
- 설치: `unity --format json command --project-path . eval_file tools/apply-sr18-encounters.cs 60000`.

설치는 이미 적·보너스가 있으면 중단하는 일회성 안전 도구다. 재실행으로 현재 수작업 배치를 덮어쓰지 않는다. 설치 전 Unity 사본 백업을 남기고 Undo 단위 안에서 검사한다. 검사 실패 시 해당 생성만 되돌리고 저장하지 않는다. 이 데이터 스냅샷을 바꾸려면 기획 SHA-256와 배치 호출을 함께 갱신해야 한다.

백업: `tmp/backups/sr18-encounters-2026-09-06/20260906-022528-893/before-encounters.unity`. 복구가 필요하면 이후 사용자 변경을 먼저 별도 보존하고 Unity를 통해 복원한다.

## 검증

- `dotnet build Assembly-CSharp-Editor.csproj -nologo`: 성공, 오류 0개. 기존 패키지/TMP 관련 경고 16개.
- `NoryangjinSr18SceneTests`: **11/11 통과**. 길·상점 보존, 실제 바닥 높이, 상하층 분리, 정상 크기, 몸체 비겹침, 스팟 링크, 보너스 등급·보존 마커, 기존 경사·회전 검사를 포함한다.
- 실제 Play Mode에서 플레이어 루트 콜라이더를 입력으로 스팟 콜백을 검사했다. 31개 스팟/69명 활성화, 중복 활성화 거부, 27명 이동 진행, 적 위치·스팟 초기화 통과. 적 접촉과 플레이어 발사는 끄고 검사하여 처치·코인 획득을 발생시키지 않았다. 물리 이동으로 전 구간을 완주한 검사는 아니다.
- 하네스 검사 통과. 원본 Map1/Map2 SHA-256 유지. 테스트·화면 촬영 뒤 SR18은 Edit Mode, clean 상태이고 디스크 SHA-256는 `818F3C8BABB29B2D7220D42D1E2AD5D9D1F66D2ED1A550C41B0CB0F527714D27`이다.
- 설치 스크립트와 씬 계약을 읽기 검토했다. 이전 작업의 변경은 보존했고, 씬은 Unity API로만 저장했다. 일반 코드의 공백 검사는 통과했다. Unity가 생성하는 씬 YAML의 빈 값 뒤 공백은 수동 정리하지 않는다.

## 플레이테스트 범위

이제 현재 맵에서 직접 테스트할 수 있다. 첫 생존 약 30초, 전체 4–5분 난이도는 아직 확정하지 않았다. 적 수가 증가했으므로 기존 챕터 보간 수치가 69명에 다시 배분된다. 효과/능력치/코인 표는 수정하지 않았다. 기존 적 사망 드롭은 별도 발생하며 14개 고정 벽과 구분한다.

강제 보너스 1택, 보스 처치 후 보상 잠금, 새 기믹 및 고속도로 전환 시스템은 추가하지 않았다. Build Settings도 유지한다. 이후 테스트에서는 이동 경로 이탈, 교차층 오발동, 보너스 동시 접촉과 실제 생존 시간을 우선 확인한다.
